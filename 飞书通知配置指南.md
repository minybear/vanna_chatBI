# 飞书点踩通知配置指南

## 问题症状
应用日志显示：`Error: LARK_WEBHOOK_URL is not set.`

## 解决步骤

### 步骤1：确认 .env 文件配置

在项目根目录的 `.env` 文件中添加（如果已添加，请检查格式）：

```bash
LARK_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/8ff07b7a-f947-4724-a73e-0d9cac275dc1
```

**注意事项：**
- ✅ `=` 前后**不要有空格**
- ✅ URL **不要用引号**包裹
- ✅ 确保这一行**单独一行**，没有其他内容
- ✅ 文件保存为 UTF-8 编码

### 步骤2：测试环境变量加载

运行测试脚本验证配置：

```bash
python test_lark_notification.py
```

**预期输出：**
- ✅ 如果配置正确：会显示"测试成功"，并在飞书群收到测试消息
- ❌ 如果配置错误：会显示具体的错误原因和解决方法

### 步骤3：重启应用

**重要：** 修改 `.env` 文件后必须重启应用才能生效！

#### 方法A：通过 IDE 重启
1. 停止当前运行的应用（点击停止按钮或 Ctrl+C）
2. 重新启动应用

#### 方法B：通过命令行重启
```bash
# 1. 查找正在运行的 Python 进程
tasklist | findstr python

# 2. 停止进程（替换 <PID> 为实际进程ID）
taskkill /PID <PID> /F

# 3. 重新启动应用
python app.py
# 或
uvicorn app:app --host 0.0.0.0 --port 8000
```

### 步骤4：验证功能

1. 启动应用后，检查启动日志，应该看到：
   ```
   ============================================================
   [ENV CHECK] 环境变量加载状态:
     - ZHIPU_API_KEY: 已设置
     - DB_HOST: 已设置
     - LARK_WEBHOOK_URL: 已设置 ✅
     - Webhook URL (前40字符): https://open.feishu.cn/open-apis/bot/...
   ============================================================
   ```

2. 在 Web UI 中测试点踩功能：
   - 提一个问题
   - 点击 "Not Helpful" 按钮
   - 检查应用日志，应该看到：
     ```
     [飞书通知] 准备发送点踩反馈...
     [飞书通知] Webhook URL 状态: 已配置 ✅
     [飞书通知] 正在发送到: https://open.feishu.cn/open-apis/bot/...
     [飞书通知] ✅ 发送成功！
     ```
   - 在飞书群中检查是否收到通知

## 常见问题排查

### 问题1：环境变量未加载

**症状：** 启动日志显示 `LARK_WEBHOOK_URL: ❌ 未设置`

**解决方法：**
1. 检查 `.env` 文件是否在项目根目录（与 `app.py` 同级）
2. 检查文件名是否正确（`.env`，不是 `.env.txt` 或其他）
3. 使用 `python test_lark_notification.py` 测试

### 问题2：飞书返回错误

**症状：** `⚠️ 飞书返回错误: {"code": 9499, "msg": "Bad Request"}`

**可能原因：**
- Webhook URL 不正确或已过期
- 机器人被移除出群
- 飞书服务暂时不可用

**解决方法：**
1. 重新生成 Webhook URL
2. 确认机器人还在群里
3. 联系飞书管理员

### 问题3：网络超时

**症状：** `❌ 请求超时，请检查网络连接`

**解决方法：**
1. 检查服务器是否能访问外网
2. 检查防火墙设置
3. 尝试使用代理（如需要）

## 代码改进说明

本次修改增强了以下功能：

1. **启动时环境变量检查**：应用启动时会打印所有关键环境变量的状态
2. **详细的错误日志**：点踩通知失败时会显示具体原因
3. **测试脚本**：提供独立的测试脚本 `test_lark_notification.py`
4. **更友好的错误提示**：当配置缺失时，会显示解决步骤

## 联系支持

如果按照以上步骤仍无法解决问题，请提供以下信息：

1. 应用启动时的完整日志（特别是 `[ENV CHECK]` 部分）
2. 点踩时的完整错误日志
3. `test_lark_notification.py` 的输出结果
4. 飞书 Webhook URL 的前50个字符（用于验证格式）
