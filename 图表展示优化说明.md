# 图表展示优化说明

## 问题分析
机器人回复的图表无法完全展示,主要存在以下问题:
1. 图表容器高度固定且过小(450px)
2. 边距设置不合理导致标签被截断
3. X轴标签重叠显示不全
4. 容器overflow属性设置为hidden导致内容被裁剪

## 优化方案

### 1. 图表容器优化 (第717行)
**修改前:**
```html
<div id="chart-container-${msgId}" style="width:100%; height:450px;"></div>
```

**修改后:**
```html
<div id="chart-container-${msgId}" style="width:100%; min-height:500px; padding:20px;"></div>
```

**改进点:**
- 使用 `min-height` 替代 `height`,允许容器根据内容自适应
- 基础最小高度提升到500px
- 添加20px内边距,给图表元素更多呼吸空间

### 2. Plotly布局配置优化 (第748-797行)

#### 2.1 边距调整
**修改前:**
```javascript
layout.margin = { l: 50, r: 20, t: 40, b: 80, pad: 4 };
```

**修改后:**
```javascript
layout.margin = { l: 80, r: 40, t: 60, b: 120, pad: 10 };
```

**改进点:**
- 左边距增加到80px,确保Y轴标签完整显示
- 右边距增加到40px
- 顶部边距增加到60px,避免标题被截断
- 底部边距增加到120px,为倾斜的X轴标签留足空间
- 内边距增加到10px

#### 2.2 X轴标签优化
**新增配置:**
```javascript
layout.xaxis.tickangle = -45;  // 标签倾斜45度避免重叠
layout.xaxis.tickmode = 'auto'; // 自动选择刻度
layout.xaxis.nticks = 15;       // 限制最多15个刻度
```

**改进点:**
- 标签倾斜显示避免长文本重叠
- 自动智能选择刻度数量
- 限制最大刻度数避免过于密集

#### 2.3 动态高度计算
**新增逻辑:**
```javascript
const dataLength = chartData.data && chartData.data[0] ? 
    (chartData.data[0].x ? chartData.data[0].x.length : chartData.data[0].y ? chartData.data[0].y.length : 10) : 10;
const minHeight = Math.max(500, dataLength * 25 + 150);
chartContainer.style.height = minHeight + 'px';
```

**改进点:**
- 根据数据点数量动态计算容器高度
- 基础高度500px,每个数据点额外增加25px
- 额外预留150px用于标题、标签和图例

#### 2.4 响应式处理
**新增:**
```javascript
.then(() => {
    window.dispatchEvent(new Event('resize'));
});
```

**改进点:**
- 图表绘制完成后触发resize事件
- 确保Plotly重新计算布局以完全适配容器

### 3. CSS样式优化

#### 3.1 容器溢出处理
**修改前:**
```css
.data-card {
    overflow: hidden;
}
```

**修改后:**
```css
.data-card {
    overflow: visible; /* 防止图表被裁剪 */
}
```

#### 3.2 图表容器样式
**新增:**
```css
.data-card [id^="chart-container-"] {
    overflow: visible !important;
}

.js-plotly-plot .plotly .modebar {
    top: 10px !important;
    right: 10px !important;
}
```

### 4. 数据表格优化 (第683行)

**修改前:**
```html
<div class="table-responsive" style="max-height: 300px; overflow-y: auto;">
```

**修改后:**
```html
<div class="table-responsive" style="max-height: 400px; overflow-y: auto; overflow-x: auto;">
<table ... style="font-size: 13px; white-space: nowrap;">
```

**改进点:**
- 表格最大高度从300px增加到400px
- 明确设置横向滚动
- 添加 `white-space: nowrap` 防止单元格内容换行
- 增加单元格内边距提升可读性

## 预期效果

1. ✅ 图表标签完整显示,不再被截断
2. ✅ X轴标签倾斜显示,避免重叠
3. ✅ 容器高度根据数据量自适应
4. ✅ 图表周围有足够的留白空间
5. ✅ 响应式布局,自动适配不同屏幕尺寸
6. ✅ 数据表格支持横向和纵向滚动
7. ✅ 整体视觉更加专业和现代化

## 测试建议

建议测试以下场景:
1. 数据点较多的柱状图(>10个柱子)
2. 标签文本较长的图表
3. 多系列的折线图
4. 包含图例的复杂图表
5. 不同屏幕尺寸下的响应式表现

## 后续优化方向

1. 可以考虑为不同图表类型(柱状图、折线图、饼图等)设置不同的默认高度
2. 可以添加全屏查看功能
3. 可以添加图表导出功能(PNG/SVG)
4. 可以优化移动端的显示效果

